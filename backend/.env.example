# DeepSeek API Key（Phase 2 启用）
DEEPSEEK_API_KEY=

# B站登录态 Cookie：浏览器登录 bilibili.com → F12 → Application → Cookies → SESSDATA 复制值
# 不配置也能跑链路，但 B站对未登录请求可能风控（返回 "啥都木有"）
BILI_SESSDATA=

# 数据库（默认 SQLite，可换 PostgreSQL）
DATABASE_URL=sqlite:///./knowledgeos.db
